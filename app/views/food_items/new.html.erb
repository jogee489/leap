<%= link_to("<< Back to list", {:action => 'index'}, :class => 'back-link') %>

<div class="food_items new">
  <h2>Create Food Items</h2>

  <%= form_for(@foodItem, :url => {:action => 'create'}) do |f| %>

  <table summary="Food Item form fields">
    <tr>
      <th>Name</th>
      <td><%= f.text_field(:name) %></td>
    </tr>
    <tr>
      <th>Category</th>
      <td><%= f.collection_select(:category_id, Category.all, :id, :name, { :prompt => 'Select a Category', :selected => @foodItem.category_id }, { class: 'form-control' }) %></td>
    </tr>
  </table>
  
  <div class="form-buttons">
    <%= submit_tag("Create Food Item") %>
  </div>

  <% end %>

  </div>

