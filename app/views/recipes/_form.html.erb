<div class="container">
	<%= add_form_element 'recipe', 'title',
		{ type: 'text'},
	  { value: @recipe.try(:title) } %>

	  <%= add_form_element 'recipe', 'ingredients',
	  { type: 'text'},
	  { value: @recipe.try(:ingredients) } %>

	  <%= add_form_element 'recipe', 'directions',
	  { type: 'text'},
	  { value: @recipe.try(:directions) } %>

	
</div>
